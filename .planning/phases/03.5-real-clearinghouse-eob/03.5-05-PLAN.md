---
phase: 03.5-real-clearinghouse-eob
plan: 05
type: execute
wave: 3
depends_on:
  - "03.5-02"
  - "03.5-03"
  - "03.5-04"
files_modified:
  - docs/adapters.md
  - docs/architecture.md
  - docs/jobs.md
  - SHOWCASE-SCRIPT.md
autonomous: false
requirements:
  - REQ-EOB-005
must_haves:
  truths:
    - "End-to-end smoke on Muath pilot data: real EOB pulled from DXC sandbox → parsed → triaged → either auto-posted (or PMS-mock-posted in dev) or flagged → notification raised, all observable in audit log + dashboard"
    - "A grep across the production code path returns ZERO references to the deterministic mock generator (mock lives only behind explicit eobMode='mock' or PAYER_MOCK_MODE=true)"
    - "docs/adapters.md DentalXChange EOB Support section accurately describes the dispatch flow, mock-vs-real precedence, credentials shape evolution, and Vercel timeout decomposition"
    - "docs/architecture.md auto-post pipeline diagram reflects Plan 04's child-job decomposition"
    - "docs/jobs.md describes the new eob_sync_child job type"
    - "SHOWCASE-SCRIPT.md (or a dedicated extension file) documents the smoke-test procedure for future regression runs"
  artifacts:
    - path: "docs/adapters.md"
      provides: "Updated DentalXChange EOB Support section reflecting Phase 3.5 reality"
      contains: "RealClearinghouseEOBSource"
    - path: "docs/architecture.md"
      provides: "Updated auto-post pipeline reflecting parent/child eob_sync decomposition"
      contains: "eob_sync_child"
    - path: "docs/jobs.md"
      provides: "eob_sync_child job type documentation"
      contains: "eob_sync_child"
    - path: "SHOWCASE-SCRIPT.md"
      provides: "Smoke-test procedure documented for future regression runs"
      contains: "Phase 3.5"
  key_links:
    - from: "docs/adapters.md DentalXChange section"
      to: "code in server/adapters/payer/clearinghouse/"
      via: "explicit references to dentalxchange.ts, dentalxchange-source-mock.ts, dentalxchange-source-real.ts, edi835-parser.ts, edi835-mappers.ts"
      pattern: "dentalxchange-source-real"
    - from: "static-grep gate"
      to: "production code path"
      via: "git grep returns empty for forbidden mock references outside the mock source file"
      pattern: "MockEOBSource|deterministic mock 835"
---

<objective>
Wave 3 — verification and documentation. Run an end-to-end smoke test on Muath's pilot data through the DXC sandbox (REQ-EOB-005-D), confirm via static analysis that no production code path references the deterministic mock generator (REQ-EOB-005-E), and update the four affected documentation files (per CLAUDE.md project documentation rule) so the codebase docs reflect Phase 3.5 reality.

Purpose: Phase 3.5's contract per ROADMAP.md is not just "code works" — it's "production grep is clean" and "docs reflect reality." This plan closes both gaps and provides a repeatable smoke-test procedure for Phase 7 pilot deploy regression.

Output: Smoke test passed (or formally documented as blocked on DXC sandbox if creds are still pending), grep gate green, four docs files updated, smoke-test runbook in SHOWCASE-SCRIPT.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.5-real-clearinghouse-eob/03.5-RESEARCH.md
@.planning/phases/03.5-real-clearinghouse-eob/03.5-VALIDATION.md
@.planning/phases/03.5-real-clearinghouse-eob/03.5-01-SUMMARY.md
@.planning/phases/03.5-real-clearinghouse-eob/03.5-02-SUMMARY.md
@.planning/phases/03.5-real-clearinghouse-eob/03.5-03-SUMMARY.md
@.planning/phases/03.5-real-clearinghouse-eob/03.5-04-SUMMARY.md
@.planning/phases/03.5-real-clearinghouse-eob/DXC-ENROLLMENT.md
@docs/adapters.md
@docs/architecture.md
@docs/jobs.md
@SHOWCASE-SCRIPT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Static grep gate — verify no production reference to mock generator</name>
  <files></files>
  <action>
Run the exact grep command from VALIDATION.md `3.5-05-02` (REQ-EOB-005-E):

```bash
git grep "MockEOBSource\|deterministic mock 835" -- 'server/**/*.ts' ':!server/adapters/payer/clearinghouse/dentalxchange-source-mock.ts'
```

This excludes the mock source file itself (where `MockEOBSource` is the class definition — that's the ONE legitimate reference). Every other match is a violation.

**Expected result:** ZERO output lines. Exit code 1 (grep returns non-zero when nothing matches).

**If matches appear, treat each one:**
- An import in `dentalxchange.ts` for the dispatcher → ALLOWED if it's the dispatch path; the `mode === 'mock'` branch needs to construct `MockEOBSource`. Allow ONLY in `dentalxchange.ts` and ONLY in the constructor branch.
- Any other match in `server/services/`, `server/adapters/pms/`, or anywhere outside `clearinghouse/` → MUST FIX. Production code must not reach `MockEOBSource` directly.

**Refined grep accounting for the legitimate import:**
```bash
git grep "MockEOBSource\|deterministic mock 835" -- \
  'server/**/*.ts' \
  ':!server/adapters/payer/clearinghouse/dentalxchange-source-mock.ts' \
  ':!server/adapters/payer/clearinghouse/dentalxchange.ts' \
  ':!server/adapters/payer/clearinghouse/dentalxchange.test.ts'
```

This refinement is acceptable because:
- `dentalxchange-source-mock.ts` defines the class
- `dentalxchange.ts` imports it for the dispatcher (single line)
- `dentalxchange.test.ts` exercises mock dispatch in unit tests (not production code path)

Run BOTH the strict version (VALIDATION.md exact) AND the refined version. Report both results in the SUMMARY. The strict version is the canonical Phase 3.5 success criterion (REQ-EOB-005-E exact wording: "A grep across the production code path returns zero references to the deterministic mock generator"). If strict fails but refined passes, document the legitimate import in the SUMMARY.

**Also run a search for the magic-number prototype tells from PATTERNS.md anti-pattern table:**
```bash
git grep "Subscriber IDs ending in '01'" -- 'server/**/*.ts' \
  ':!server/adapters/payer/clearinghouse/dentalxchange.ts'
```
Should return EMPTY — that magic only lives in `checkEligibility` (out of Phase 3.5 scope but verified-not-leaked).

```bash
git grep "simulated latency" -- 'server/**/*.ts' \
  ':!server/adapters/payer/clearinghouse/dentalxchange.ts' \
  ':!server/adapters/payer/clearinghouse/dentalxchange-source-mock.ts'
```
Should return EMPTY — `await new Promise((r) => setTimeout(r, 200))` patterns must only be in mock files.
  </action>
  <verify>
    <automated>! git grep -E "MockEOBSource|deterministic mock 835" -- 'server/**/*.ts' ':!server/adapters/payer/clearinghouse/dentalxchange-source-mock.ts' ':!server/adapters/payer/clearinghouse/dentalxchange.ts' ':!server/adapters/payer/clearinghouse/dentalxchange.test.ts'</automated>
  </verify>
  <done>Strict grep run; result documented in SUMMARY. Refined grep returns zero output (exit code 1). Magic-prototype-tell greps both return zero. Any deviation is explained in the SUMMARY with file/line/justification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end smoke test on Muath pilot data via DXC sandbox</name>
  <what-built>
Full Phase 3.5 stack: dispatcher (Plan 02), parser + mapper (Plan 03), real HTTP client + child-job decomposition + Vercel timeout config (Plan 04). Per VALIDATION.md `3.5-05-01` this is manual-only because it requires real DXC sandbox creds + real Muath payer enrollment.
  </what-built>
  <how-to-verify>
**Pre-conditions:**
1. `.planning/phases/03.5-real-clearinghouse-eob/DXC-ENROLLMENT.md` shows `sandbox_creds_received: true` AND at least one Muath pilot payer (Delta Dental | MetLife | Cigna) is enrolled for ERA delivery in the DXC sandbox.
2. `.env.local` has `DXC_SANDBOX_API_KEY` and `DXC_SANDBOX_API_BASE_URL` set.
3. `payer_configs` row for Muath's clinic + that payer has `featuresEnabled = { eob: true, eobMode: 'sandbox' }` and credentials encrypted via `services/encryption/credentials.encrypt()`.

**Smoke test procedure (run in order):**

1. **Confirm dev DB has the test data:**
   ```bash
   npx tsx server/db/seed.ts  # if needed; or use existing Muath seed
   ```

2. **Trigger eob_sync via the API:**
   ```bash
   curl -X POST http://localhost:3000/api/eob/sync \
     -H "Authorization: Bearer <staff_admin_jwt>" \
     -H "Content-Type: application/json" \
     -d '{ "clinic_id": "<muath-clinic-uuid>" }'
   ```
   Expected: 200 with `{ jobId: "<parent-job-uuid>" }`.

3. **Drive the worker (single-step):**
   ```bash
   curl -X POST http://localhost:3000/api/internal/worker \
     -H "Authorization: Bearer ${JWT_SECRET}"
   ```
   First call processes the parent `eob_sync` job. Subsequent calls process each `eob_sync_child` job. Repeat until `getNextJob()` returns null.

4. **Verify expected effects in Postgres:**
   - `SELECT * FROM jobs WHERE clinic_id = '<muath>' ORDER BY created_at DESC LIMIT 10;`
     - One `eob_sync` parent (status=completed, processedItems = N children)
     - N `eob_sync_child` jobs (one per enabled payer config, all status=completed)
   - `SELECT * FROM eob_records WHERE clinic_id = '<muath>' AND created_at > now() - interval '5 minutes';`
     - One row per real ERA pulled from sandbox
     - `source = 'edi_835'`
     - `triage_status` is one of: `auto_post`, `flagged`, `manually_posted`, `skipped`
     - `line_items` JSONB has at least one entry per row
   - `SELECT * FROM notifications WHERE clinic_id = '<muath>' AND type IN ('eob_flagged') ORDER BY created_at DESC;`
     - At least one row IF any EOB was flagged (denial / over-threshold / mismatched / etc.)
   - `SELECT * FROM audit_log WHERE clinic_id = '<muath>' AND created_at > now() - interval '5 minutes';`
     - Rows for each API call. NO PHI in `details` column.

5. **Confirm dashboard reflects the new EOBs:**
   - Visit `http://localhost:3000/eob` as `staff_admin@muath`
   - Recently created EOBs appear in the list with the correct payer name and check number
   - Click into a flagged one (if any) — review screen renders with line items

6. **Confirm the auto-post path (or its mock equivalent if PMS keys are 'placeholder'):**
   - Auto-eligible EOBs (passed all 6 triage rules) show `posted_to_pms = true`
   - When `pms_config.developer_key` decrypts to `'placeholder'` (D-015), `pms_claim_payment_id` is `MOCK-<timestamp>` — not a real OD claim ID

**Pass criteria (all must hold):**
- [ ] At least one `eob_records` row exists with `source = 'edi_835'` from the test run
- [ ] Each row's `line_items[].procedure_code` matches a CDT pattern (`/^D\d{4}$/`)
- [ ] No row has `triage_status = NULL` (every record was triaged)
- [ ] Worker logs do not contain raw EDI text or 9+ digit numerical runs (PHI scrub regression)
- [ ] No job has status `failed` for an unexpected reason (rate limit / timeout reasons are acceptable; PHI in errors is NOT)

**Failure mode handling:**
- If sandbox returns 0 ERAs in the date range: extend range to 90 days; if still 0, this means Muath's payer enrollment in DXC sandbox is incomplete — block on partner team and document in DXC-ENROLLMENT.md.
- If parser throws on real ERAs: capture the offending fixture (anonymized via Plan 01 README recipe) and add it to `__fixtures__/`. Update `edi835-mappers.test.ts` to cover the new shape. Re-run.
- If worker times out on the first ERA backlog: confirm Plan 04 child-job decomposition is wired correctly (parent should finish in &lt;5s).

**Sandbox blocked? Document and proceed.** If DXC sandbox is still unavailable, this checkpoint is BLOCKED. Document in `03.5-05-SUMMARY.md` and in `DXC-ENROLLMENT.md` that smoke test is pending. The remaining Wave 3 tasks (grep gate Task 1, docs Tasks 3-4) are NOT blocked — they ship independently. Phase verify can sign off conditionally with a note that smoke test is gated on partner enrollment.
  </how-to-verify>
  <resume-signal>
Type "smoke pass" if all pass criteria hold. Type "smoke blocked: dxc-sandbox-not-ready" if DXC enrollment is incomplete (acceptable; Phase 7 pilot deploy will re-run this). Type "smoke fail: <reason>" with details if a parser/triage/notification issue surfaces — that may require returning to Wave 1 or Wave 2 for a fix.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update docs/adapters.md DentalXChange EOB Support section</name>
  <files>docs/adapters.md</files>
  <action>
Read the current `docs/adapters.md` first (the working tree has uncommitted edits from Phase 3 EOB completion per `git status`). Find the "DentalXChange EOB Support" section (or equivalent — search for "DentalXChange" header).

Replace the section's body with content reflecting Phase 3.5 reality. The section MUST cover:

1. **Mock-vs-real dispatch (mirroring D-015 OpenDental precedent):**
   - `featuresEnabled.eobMode` JSONB field on `payer_configs`: `'mock' | 'sandbox' | 'production'`
   - Precedence (in order): `featuresEnabled.eob === false` skips entirely → `PAYER_MOCK_MODE === 'true'` → missing/'placeholder' credentials → explicit `eobMode` → default `'mock'`
   - Where each is enforced (engine.ts for `eob: false` check; adapter `resolveMode` for everything else)

2. **Architecture (file map):**
   ```
   server/adapters/payer/clearinghouse/
   ├── dentalxchange.ts                     # adapter shell — dispatches to source
   ├── dentalxchange-source-mock.ts         # MockEOBSource — deterministic 835 generator (DEV ONLY)
   ├── dentalxchange-source-real.ts         # RealClearinghouseEOBSource — DXC HTTP client
   ├── edi835-parser.ts                     # parse835(rawEdi) — wraps x12-parser
   ├── edi835-mappers.ts                    # mapParsed835ToRawEOB — pure function, BPR/CLP reconciliation
   ├── edi835-types.ts                      # Parsed835 segment-tree types
   ├── source-types.ts                      # IClearinghouseEOBSource interface
   ├── utils.ts                             # simpleHash (used by mock only)
   └── __fixtures__/                        # anonymized 835 samples for unit tests
   ```

3. **Credentials shape evolution (RESEARCH.md Pitfall 6):**
   - Legacy: `payer_configs.credentials` was a single encrypted string (treated as `{ eligibility: { apiKey: <plaintext> } }`)
   - New: `payer_configs.credentials` may be an encrypted JSON blob `{ eligibility?: { apiKey }, payment?: { apiKey, baseUrl? } }`
   - Both shapes are supported by `decryptDXCCredentials()` in `server/adapters/payer/registry.ts`
   - DXC may issue distinct credentials for Eligibility API vs Payment API — the JSON shape supports both surfaces

4. **Operational decomposition (RESEARCH.md Pitfall 2):**
   - `eob_sync` parent job enqueues one `eob_sync_child` per enabled `payer_configs` row
   - Each child processes ONE (clinicId, payerConfigId, dateRange) tuple to stay under Vercel's 300s function timeout
   - Idempotency via existing `(clinicId, checkNumber, payerName)` uniqueness on `eob_records`

5. **Anonymization recipe link:**
   - Reference `server/adapters/payer/clearinghouse/__fixtures__/README.md` for fixture standards
   - Statement: "No real PHI may enter the `__fixtures__/` directory."

6. **Required env vars (production):**
   - `DXC_PAYMENT_API_BASE_URL` — production XConnect Payment API base URL
   - `DXC_SANDBOX_API_BASE_URL` — sandbox base URL
   - `DXC_API_OAUTH_TOKEN_URL` — OAuth2 token endpoint (only if auth scheme is OAuth)
   - All env vars are `.optional()` in the Zod config so dev environments without DXC creds boot cleanly

7. **Testing pattern:**
   - Unit tests: `*.test.ts` co-located with source files (Vitest, Node env, no path aliases)
   - Integration test: `dentalxchange.real.test.ts` runs against DXC sandbox when `DXC_SANDBOX_API_KEY` is set; auto-skipped otherwise
   - Fixture-driven tests for parser + mappers — see `edi835-mappers.test.ts` and `__fixtures__/*.835.txt`

Preserve any existing prose for `IPMSAdapter` / OpenDental adapter — Phase 3.5 only updates the DentalXChange/EOB section.

Per CLAUDE.md project rule: documentation is the primary onboarding/debugging reference. Don't leave it stale.
  </action>
  <verify>
    <automated>grep -q "RealClearinghouseEOBSource" docs/adapters.md &amp;&amp; grep -q "eobMode" docs/adapters.md &amp;&amp; grep -q "edi835" docs/adapters.md</automated>
  </verify>
  <done>`docs/adapters.md` DentalXChange EOB Support section accurately describes the dispatcher, file structure, credentials shape evolution, child-job decomposition, env vars, and testing pattern. The section references real file paths that exist in the repo. No leftover language about "mock 835 generator pending production swap" remains.</done>
</task>

<task type="auto">
  <name>Task 4: Update docs/architecture.md, docs/jobs.md, and SHOWCASE-SCRIPT.md</name>
  <files>
docs/architecture.md,
docs/jobs.md,
SHOWCASE-SCRIPT.md
  </files>
  <action>
**Edit 1 — `docs/architecture.md`:** Find the auto-post pipeline section (search for "auto-post" or "EOB"). Update the pipeline diagram + prose to reflect parent/child job decomposition:

```
Vercel Cron (*/1)
  → POST /api/internal/worker  [maxDuration=300s]
    → processNextJob()
      → eob_sync (parent)              # enumerates enabled payer_configs, enqueues children
        → eob_sync_child × N            # one per (clinic, payer_config, dateRange)
          → DentalXChangeAdapter.retrieveEOBs
            → MockEOBSource OR RealClearinghouseEOBSource (per featuresEnabled.eobMode)
              → [real path] DXC API → 835 EDI → parse835 → mapParsed835ToRawEOB
            → engine: dedupe by (clinicId, checkNumber, payerName) → triage → notify
          → autoPostEligibleEobs (per child, bounded scope)
```

Add a note: "Phase 3.5 introduced the parent/child decomposition to keep individual function invocations under Vercel's 300s limit. The previous monolithic `eob_sync` (Phase 3) is preserved as a fallback path for dev/manual triggers."

**Edit 2 — `docs/jobs.md`:** Find the job types table/list. Add `eob_sync_child` with description:

| Job Type | Trigger | Description |
|----------|---------|-------------|
| `eob_sync_child` | Enqueued by `eob_sync` parent | Processes ONE `(clinicId, payerConfigId, dateRange)` tuple. Bounded work to stay under Vercel's 300s function timeout. Idempotent via existing `eob_records` uniqueness. |

Update the existing `eob_sync` description to clarify it's now a parent that enqueues children rather than doing the work itself.

**Edit 3 — `SHOWCASE-SCRIPT.md`:** Append a new section "Phase 3.5 Smoke Test (Pilot Regression)" with the procedure documented in Plan 05 Task 2 (above):
- Pre-conditions checklist (DXC creds, payer enrollment, env vars)
- Step-by-step smoke procedure (curl commands, SQL queries, dashboard checks)
- Pass criteria (5 bullet points)
- Failure mode handling

This section serves as the Phase 7 pilot regression script — re-run before pilot deploy on production data.

Per CLAUDE.md project rule: every code change must surface in docs. Phase 3.5 touches the EOB pipeline architecture, the job types, the adapter contract, AND the showcase demo — all four files need updates.

Phase 6 will revisit `docs/deployment.md` for HIPAA controls and Phase 5 reconciles Azure → Vercel + Neon prose. Those are out of scope here.
  </action>
  <verify>
    <automated>grep -q "eob_sync_child" docs/architecture.md &amp;&amp; grep -q "eob_sync_child" docs/jobs.md &amp;&amp; grep -q "Phase 3.5" SHOWCASE-SCRIPT.md</automated>
  </verify>
  <done>`docs/architecture.md` shows updated pipeline diagram with parent/child decomposition. `docs/jobs.md` lists `eob_sync_child`. `SHOWCASE-SCRIPT.md` has a Phase 3.5 smoke-test section with pre-conditions, procedure, pass criteria, and failure handling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Production code path ↔ Mock generator | Must remain disjoint after Phase 3.5 — verified by grep gate (Task 1) |
| Smoke test environment ↔ DXC sandbox | Real PHI may be in sandbox responses (test data) — no logging of raw EDI |
| Documentation ↔ Reality | Stale docs cause regressions later — explicit doc updates per code change (CLAUDE.md project rule) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3.5-MOCK-LEAK | Tampering | Production code reaching `MockEOBSource` | mitigate | Task 1 grep gate is run; refined version excludes only the legitimate import sites (mock source file, dispatcher, mock test). Any additional reference is a bug — must be fixed before phase verify. |
| T-3.5-PHI-SMOKE | Information Disclosure | Smoke test logs | mitigate | Smoke procedure (Task 2) explicitly checks worker logs for raw EDI / 9+ digit runs. PHI scrub from Plan 03 (`PHI_SCRUB` regex) catches parser errors. The `errorHandler` middleware deeper audit is deferred to Phase 6 — Phase 3.5 surfaces but does not regress. |
| T-3.5-DOC-DRIFT | Repudiation | Stale docs | mitigate | Tasks 3-4 update all four affected doc files in lockstep. Verify commands grep for the new vocabulary in each. |
</threat_model>

<verification>
- `! git grep -E "MockEOBSource|deterministic mock 835" -- 'server/**/*.ts' ':!<allow-list>'` returns empty
- `grep -q "RealClearinghouseEOBSource" docs/adapters.md` — found
- `grep -q "eob_sync_child" docs/architecture.md docs/jobs.md` — both found
- `grep -q "Phase 3.5" SHOWCASE-SCRIPT.md` — found
- Smoke test passed OR formally documented as blocked-on-DXC in DXC-ENROLLMENT.md
- Full Vitest run (`npx vitest run`) — all tests across all plans green
</verification>

<success_criteria>
- REQ-EOB-005-D: end-to-end smoke executed (or formally blocked on DXC sandbox availability)
- REQ-EOB-005-E: production grep returns zero references to mock generator (refined allow-list excludes only legitimate import sites)
- `docs/adapters.md`, `docs/architecture.md`, `docs/jobs.md`, `SHOWCASE-SCRIPT.md` all reflect Phase 3.5 reality
- No PHI in worker logs during smoke test (regression guard for Phase 6 audit)
- All Phase 3.5 unit + integration tests still green (no regressions from doc edits)
</success_criteria>

<output>
After completion, create `.planning/phases/03.5-real-clearinghouse-eob/03.5-05-SUMMARY.md` recording:
- Strict grep result + refined grep result
- Smoke test status (pass | blocked | fail) with evidence (job IDs, EOB IDs, screenshot paths if applicable)
- Documentation files modified with section diff summaries
- Any regression that surfaced and how it was handled
- Phase 3.5 verification readiness — green / yellow / red — with explicit gates that gate `/gsd-verify-work` sign-off
</output>
