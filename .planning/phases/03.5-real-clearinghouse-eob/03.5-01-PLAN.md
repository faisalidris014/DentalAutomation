---
phase: 03.5-real-clearinghouse-eob
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - server/adapters/payer/clearinghouse/__fixtures__/cms-sample-1.835.txt
  - server/adapters/payer/clearinghouse/__fixtures__/delta-dental-anonymized.835.txt
  - server/adapters/payer/clearinghouse/__fixtures__/metlife-anonymized.835.txt
  - server/adapters/payer/clearinghouse/__fixtures__/cigna-anonymized.835.txt
  - server/adapters/payer/clearinghouse/__fixtures__/non-default-delimiters.835.txt
  - server/adapters/payer/clearinghouse/__fixtures__/denial-cas.835.txt
  - server/adapters/payer/clearinghouse/__fixtures__/plb-takeback.835.txt
  - server/adapters/payer/clearinghouse/__fixtures__/README.md
  - .planning/phases/03.5-real-clearinghouse-eob/DXC-ENROLLMENT.md
autonomous: false
requirements:
  - REQ-EOB-005
must_haves:
  truths:
    - "Vitest runs `npx vitest --version` and prints a version"
    - "`npm test` exits 0 on an empty test suite (no tests, no errors)"
    - "Fixtures directory exists with at minimum 7 anonymized 835 sample files (CMS generic + Delta + MetLife + Cigna + non-default delimiters + denial CAS + PLB takeback)"
    - "Fixture anonymization recipe is documented and discoverable from docs/adapters.md"
    - "DXC partner enrollment status is tracked with an explicit owner, contact path, and escalation deadline"
  artifacts:
    - path: "package.json"
      provides: "vitest devDependency + test/test:watch scripts"
      contains: "vitest"
    - path: "vitest.config.ts"
      provides: "Node test environment, default include glob, no path aliases"
      min_lines: 5
    - path: "server/adapters/payer/clearinghouse/__fixtures__/README.md"
      provides: "Fixture anonymization recipe (DOB → 1980-01-01, insurance ID → INS-TEST-NNN, SSN scrub, name pool)"
      min_lines: 20
    - path: ".planning/phases/03.5-real-clearinghouse-eob/DXC-ENROLLMENT.md"
      provides: "Partner enrollment tracker — contact, status, ETA, escalation path"
      min_lines: 15
  key_links:
    - from: "package.json"
      to: "vitest.config.ts"
      via: "test script invokes vitest run which reads vitest.config.ts"
      pattern: "\"test\":\\s*\"vitest run\""
    - from: "server/adapters/payer/clearinghouse/__fixtures__/"
      to: "Wave 1 mapper tests"
      via: "fs.readFileSync from edi835-mappers.test.ts"
      pattern: ".835.txt"
---

<objective>
Stand up Wave 0 infrastructure for Phase 3.5: install Vitest as the test runner (no test framework exists in this repo today), commit anonymized 835 fixtures that the parser/mapper unit tests will consume in Wave 1, and create a non-code tracker for the DentalXChange partner enrollment that gates Wave 2.

Purpose: Wave 1 needs `vitest run` to execute and at minimum one anonymized 835 fixture per scenario the mappers must handle. Wave 2 (real DXC HTTP client + integration tests) cannot start until DXC sandbox credentials arrive — surfacing this as an owned, dated tracker prevents the phase from silently stalling.

Output: working test harness, 7 fixture files + README documenting anonymization, DXC enrollment tracker with named owner.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.5-real-clearinghouse-eob/03.5-RESEARCH.md
@.planning/phases/03.5-real-clearinghouse-eob/03.5-VALIDATION.md
@.planning/phases/03.5-real-clearinghouse-eob/03.5-PATTERNS.md
@package.json
@docs/adapters.md

<interfaces>
<!-- Existing scripts in package.json (lines 5-14) -->
"scripts": {
  "dev": "next dev",
  "build": "next build",
  "start": "next start",
  "db:generate": "drizzle-kit generate",
  "db:migrate": "tsx server/db/migrate.ts",
  "db:push": "drizzle-kit push",
  "db:seed": "tsx server/db/seed.ts",
  "db:studio": "drizzle-kit studio"
}

<!-- Existing dependencies (verified via Read package.json) -->
- TypeScript 5
- Next.js 16.2.2
- Zod 4.3.6
- Drizzle ORM 0.45.2
- No test framework currently installed
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Vitest and add npm scripts</name>
  <files>package.json, vitest.config.ts</files>
  <action>
Run `npm install --save-dev vitest@^1` to add Vitest to devDependencies. Verify via `npm view vitest@^1 version` first if version `^1` is unclear; latest stable v1.x as of 2026-04-26 is acceptable.

Add to `scripts` block in `package.json` (preserve existing scripts verbatim, add these two):
```json
"test": "vitest run",
"test:watch": "vitest"
```

Create `vitest.config.ts` at repo root with minimal config:
```typescript
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    environment: 'node',
    include: ['server/**/*.test.ts', 'lib/**/*.test.ts'],
    testTimeout: 10000,
  },
});
```

Rationale per PATTERNS.md §"vitest.config.ts": codebase uses relative imports (no `@/` aliases), so Vitest needs no path alias config. Node environment because tests target server-side code (no DOM). Default include glob covers co-located `*.test.ts` next to source per RESEARCH.md §"Recommended Project Structure" line 174.

Do NOT add Jest, ts-jest, @testing-library, or jsdom — they are not needed and would inflate install size.
  </action>
  <verify>
    <automated>npx vitest --version &amp;&amp; npm test 2>&amp;1 | grep -E "(No test files|0 passed|exit code 0)"</automated>
  </verify>
  <done>`npx vitest --version` prints a 1.x.y version. `npm test` exits 0 with no test files (Vitest treats empty suite as success). `package.json` has `"test": "vitest run"` and `"test:watch": "vitest"` scripts. `vitest.config.ts` exists at repo root with Node environment.</done>
</task>

<task type="auto">
  <name>Task 2: Create anonymized 835 fixtures and anonymization recipe</name>
  <files>
server/adapters/payer/clearinghouse/__fixtures__/cms-sample-1.835.txt,
server/adapters/payer/clearinghouse/__fixtures__/delta-dental-anonymized.835.txt,
server/adapters/payer/clearinghouse/__fixtures__/metlife-anonymized.835.txt,
server/adapters/payer/clearinghouse/__fixtures__/cigna-anonymized.835.txt,
server/adapters/payer/clearinghouse/__fixtures__/non-default-delimiters.835.txt,
server/adapters/payer/clearinghouse/__fixtures__/denial-cas.835.txt,
server/adapters/payer/clearinghouse/__fixtures__/plb-takeback.835.txt,
server/adapters/payer/clearinghouse/__fixtures__/README.md
  </files>
  <action>
Create `server/adapters/payer/clearinghouse/__fixtures__/` directory with 7 fixture files. Use file extension `.835.txt` per PATTERNS.md §"Open Conventions" item 3 (chosen for browse readability — establishes the convention for the rest of the phase). Hand-craft fixtures from the 835 5010 X221A1 reference structure shown in RESEARCH.md §"Code Examples" (line 411 onwards).

Required scenario coverage (per VALIDATION.md Wave 0 Requirements + RESEARCH.md §"Wave 0 Gaps"):

1. **cms-sample-1.835.txt** — generic 5010 sample modeled on the CMS/Nevada Medicaid public sample (`medicaid.nv.gov/Downloads/provider/Sample_835_File.pdf` per RESEARCH.md §"Environment Availability"). Single CLP, single SVC. Default delimiters (`*` element, `~` segment, `:` component).

2. **delta-dental-anonymized.835.txt** — Delta Dental of MN payer (N1*PR*DELTA DENTAL OF MN), Bright Smiles Dental payee (NPI 1234567890), 2 patients × 2 procedures each (D0120 prophylaxis, D2740 crown). Uses `AD:` qualifier on SVC01 for ADA dental codes.

3. **metlife-anonymized.835.txt** — MetLife payer, single patient, denial scenario (CAS*PR*1*100 for deductible), waiting period rejection (CAS*CO*45*80).

4. **cigna-anonymized.835.txt** — Cigna payer, single patient, copay scenario (CAS*PR*3*25 for copay).

5. **non-default-delimiters.835.txt** — same content as cms-sample-1 but ISA segment uses `|` element separator, `\n` segment terminator. Verifies parser reads delimiters from ISA per RESEARCH.md Pitfall 3.

6. **denial-cas.835.txt** — claim with multiple CAS segments (PR=patient resp, CO=contractual obligation, OA=other), denial code CO-4 ("Procedure code inconsistent with modifier"). Tests Pitfall 5 / dental tooth-number handling.

7. **plb-takeback.835.txt** — claim with PLB segment (provider-level adjustment, WO=withhold/recoupment, negative amount). Tests RESEARCH.md anti-pattern: "BPR02 = sum(CLP04) + net(PLB)" reconciliation.

**Anonymization rules (apply to ALL fixtures):**
- Insurance/subscriber IDs: `INS-TEST-001`, `INS-TEST-002`, ...
- Patient names: pool of 5 fictional names (Sarah Johnson, Michael Thompson, James Davis, Emily Martinez, Robert Wilson) — already used in mock generator at `dentalxchange.ts:187-188`
- DOBs: ISO date `1980-01-01` (single deterministic value)
- SSN-shaped digit runs: NEVER include any 9-digit run that looks like an SSN
- NPIs: `1234567890` (clinic), `9876543210` (rendering provider) — both literal test values
- Provider/clinic names: `BRIGHT SMILES DENTAL`, `DR PROVIDER` — fictional
- Check numbers: `DEL-100001`, `MET-200001`, etc.
- Trace numbers: `TRACE-100001`, ...

Create `server/adapters/payer/clearinghouse/__fixtures__/README.md` documenting:
- Why these fixtures exist (Wave 1 unit tests for `edi835-mappers.test.ts` and `dentalxchange.test.ts`)
- Anonymization recipe (the rules above as a copy-paste-ready table)
- Process for adding a new fixture (download from DXC sandbox once available → run anonymizer → commit)
- Statement that fixtures are HAND-CRAFTED for the demo phase; once DXC sandbox is live, replace with anonymized real samples per payer
- Explicit promise: "No real PHI has ever entered this directory. If a real 835 is needed for testing, it MUST be anonymized using the recipe in this file before commit."

This addresses threat T-3.5-PHI-1: even fixtures must not leak PHI.
  </action>
  <verify>
    <automated>ls server/adapters/payer/clearinghouse/__fixtures__/*.835.txt | wc -l | grep -q 7 &amp;&amp; test -f server/adapters/payer/clearinghouse/__fixtures__/README.md &amp;&amp; ! grep -rE '\b[0-9]{9}\b' server/adapters/payer/clearinghouse/__fixtures__/*.835.txt | grep -v -E '(1234567890|9876543210)' | grep -q .</automated>
  </verify>
  <done>7 `.835.txt` fixture files exist covering all required scenarios. README.md documents anonymization recipe. No fixture contains a 9-digit run other than the two known test NPIs (1234567890, 9876543210). Each fixture is parseable as text (not binary).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Open DXC partner enrollment + create tracker</name>
  <what-built>
N/A — this is a real-world, human-only step. DXC partner enrollment cannot be automated by Claude. The `developer.dentalxchange.com` portal is gated behind login/registration per RESEARCH.md §Open Question 1 (line 559), and partner-tier API access typically requires a sales conversation.
  </what-built>
  <how-to-verify>
The user (Faisal / Sami / Muath ops) must complete the following BEFORE Wave 2 starts. Wave 1 is NOT blocked by this task — it can proceed in parallel.

1. Visit https://developer.dentalxchange.com/ and create or use an existing partner account.
2. Request access to the **XConnect Payment API** (the ERA / 835 retrieval surface).
3. Capture from the partner team:
   - Sandbox base URL (`DXC_SANDBOX_API_BASE_URL`)
   - Production base URL (`DXC_PAYMENT_API_BASE_URL`)
   - Auth scheme (API key, OAuth2 client_credentials, or Bearer token) — RESEARCH.md §Open Question 3
   - Whether the API returns raw 835 EDI or pre-parsed JSON — RESEARCH.md §Open Question 2
   - Whether one credential set covers Eligibility + Payment APIs or each surface has separate credentials — RESEARCH.md §Pitfall 6
   - ERA enrollment status for Muath's pilot payers (Delta Dental, MetLife, Cigna) — RESEARCH.md §Open Question 5
4. Save credentials to a password manager. Do NOT commit them.

Create `.planning/phases/03.5-real-clearinghouse-eob/DXC-ENROLLMENT.md` with this exact frontmatter and body (Claude must create the FILE; the user fills in the values during/after the call):

```markdown
---
status: open
owner: <Faisal | Sami | Muath ops>
opened: 2026-04-26
sandbox_creds_received: false
prod_creds_received: false
escalate_after: 2026-05-10  # 2 weeks
fallback: "Switch to Stedi (RESEARCH.md §Open Question 6) — JSON-only response, eliminates EDI parser path"
---

# DXC Partner Enrollment Tracker

## Contact
- Portal: https://developer.dentalxchange.com/
- Partner email: TBD
- Sales contact: TBD

## Status Log
- 2026-04-26 — Plan created, enrollment not yet requested.

## Required Information (fill as it arrives)
- [ ] Sandbox base URL: TBD
- [ ] Production base URL: TBD
- [ ] Auth scheme: TBD (API key / OAuth2 / Bearer)
- [ ] Response format: TBD (raw 835 EDI / pre-parsed JSON)
- [ ] Credentials shape: TBD (single key / per-API-surface)
- [ ] Muath pilot ERA enrollment — Delta Dental: TBD
- [ ] Muath pilot ERA enrollment — MetLife: TBD
- [ ] Muath pilot ERA enrollment — Cigna: TBD

## Escalation Path
If sandbox credentials have not arrived by `escalate_after`:
1. User decides: continue waiting, OR pivot to Stedi as Plan B (`/gsd-plan-phase --replan` to swap real-source).
2. Wave 2 tasks remain blocked until decision.
3. Wave 1 + Wave 0 + Wave 3 docs/static-grep can land independently.
```
  </how-to-verify>
  <resume-signal>
Type "tracker created" once `DXC-ENROLLMENT.md` exists with the user's chosen owner. Enrollment itself does NOT need to be complete to resume — Wave 1 proceeds against the abstracted `IClearinghouseEOBSource` interface regardless. Only Wave 2 (Plan 04) is gated on `sandbox_creds_received: true`.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository ↔ committed files | Anything in `__fixtures__/` becomes part of the public repo history forever — must contain zero real PHI |
| Local dev ↔ DXC partner portal | Credentials obtained during enrollment must never be committed to git or pasted in plans/tickets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3.5-PHI-1 (fixture scope) | Information Disclosure | `__fixtures__/*.835.txt` | mitigate | Hand-craft fixtures from public 5010 reference; explicit anonymization recipe in `__fixtures__/README.md`; verify command in Task 2 greps for any unknown 9-digit runs |
| T-3.5-CRED-1 (enrollment scope) | Information Disclosure | `DXC-ENROLLMENT.md` | mitigate | Tracker file holds only metadata (status, contact email) — never credentials. Credentials go to password manager + Vercel env vars, never committed |
| T-3.5-FIXTURE-LEAK | Information Disclosure | git history | mitigate | Task 2 verify command runs grep for SSN-shaped digit runs before fixtures are committed; commit-time grep gate prevents accidental leak |
</threat_model>

<verification>
Run `npx vitest --version` — must print a version. Run `npm test` — must exit 0 with no test files. Run `ls server/adapters/payer/clearinghouse/__fixtures__/*.835.txt | wc -l` — must equal 7. Verify `__fixtures__/README.md` documents the anonymization recipe. Verify `DXC-ENROLLMENT.md` exists with named owner.
</verification>

<success_criteria>
- Vitest runnable end-to-end (`npm test` exits 0 on empty suite)
- 7 anonymized 835 fixtures committed covering: generic single-CLP, Delta Dental, MetLife, Cigna, non-default delimiters, denial CAS, PLB takeback
- Fixture anonymization recipe documented and discoverable
- DXC enrollment tracker exists with explicit owner and 2-week escalation
- No 9-digit numerical runs in fixtures except the two known test NPIs (1234567890, 9876543210)
</success_criteria>

<output>
After completion, create `.planning/phases/03.5-real-clearinghouse-eob/03.5-01-SUMMARY.md` recording: Vitest version installed, fixture file list, anonymization recipe link, DXC enrollment tracker URL, and the chosen owner.
</output>
